<my-app></my-app>
  <!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <title>Derma Glow Aesthetics</title>
  <!-- Favicon -->
  <link rel="icon" href="/images/facevue%20logo_Website-02.png" type="image/png">
  <style>
    body {
      background-color: #FBF5E5; /* Light background color for the entire page */
    }
    .founder-section img {
      max-width: 100%; /* Make sure the image is responsive */
      height: auto; /* Maintain aspect ratio */
    }
    .founder-info {
      display: flex;
      flex-direction: column;
      justify-content: center; /* Center the content vertically */
    }
    .carousel-item img {
      height: 400px; /* Set a fixed height for images */
      object-fit: cover; /* Ensure images cover the area without distortion */
    }
    .carousel-caption {
      bottom: 20px; /* Position the caption slightly above the bottom */
    }
    .carousel-indicators button {
      background-color: #000; /* Dark color for indicators */
    }
    .carousel-control-prev-icon,
    .carousel-control-next-icon {
      background-color: #000; /* Dark color for control icons */
    }
    .btn-primary {
      background-color: #000; /* Button background color */
      border-color: #000; /* Button border color */
    }
    .btn-primary:hover {
      background-color: #A61B2A; /* Darker shade on hover */
      border-color: #A61B2A; /* Border color on hover */
    }
  </style>
</head>
<body class="p-3 m-0 border-0 bd-example">
  <!-- Founder Section -->
<div class="container my-5">
  <div class="row founder-section">
    <div class="col-md-6">
      <img src="/images/home-3-after (2).webp" alt="Founder Image" class="img-fluid rounded-circle founder-image">
    </div>
    <div class="col-md-6 founder-info">
      <h3>Our Founder</h3>
      <p>Meet our visionary founder who has been the driving force behind our success. With a passion for excellence and a dedication to providing the best services, our founder has led us to new heights.</p>
    </div>
  </div>
</div>
  <!--before after slider-->
  <script src="https://cdn.commoninja.com/sdk/latest/commonninja.js" defer></script>
<div class="commonninja_component pid-cfd0cdfb-a134-4f08-94e1-b12e99560fcb"></div>
  
     

    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>

    <!-- Bootstrap JS -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
     
    <script>
      $(document).ready(function() {
          var $slider = $('.before-after-slider');
          var $handle = $slider.find('.handle');
          var $sliderImg = $slider.find('.slider img');
          
          var dragging = false;

          function updateSlider(e) {
              var offsetX = e.pageX - $slider.offset().left;
              if (offsetX < 0) offsetX = 0;
              if (offsetX > $slider.width()) offsetX = $slider.width();
              
              $sliderImg.css('width', offsetX + 'px');
              $handle.css('left', offsetX + 'px');
          }

          $handle.on('mousedown', function() {
              dragging = true;
          });

          $(document).on('mouseup', function() {
              dragging = false;
          });

          $(document).on('mousemove', function(e) {
              if (dragging) {
                  updateSlider(e);
              }
          });

          // Initialize slider to center
          updateSlider({ pageX: $slider.offset().left + $slider.width() / 2 });
      });
  </script>
    </div>
  